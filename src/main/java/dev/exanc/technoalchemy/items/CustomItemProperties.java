package dev.exanc.technoalchemy.items;

import dev.exanc.technoalchemy.TechnoAlchemical;
import dev.exanc.technoalchemy.component.DataComponentTypes;
//import net.minecraft.client.renderer.item.ItemProperties;
import net.minecraft.resources.ResourceLocation;

public class CustomItemProperties {

  /*public static void register() {
    ItemProperties.register(
      Items.CHISEL.get(),
      ResourceLocation.fromNamespaceAndPath(TechnoAlchemical.MOD_ID, "used"),
      (stack, level, entity, seed) -> stack.get(DataComponentTypes.COORDINATES) != null ? 1 : 0
    );
  }*/
}
